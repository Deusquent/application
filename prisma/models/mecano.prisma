model Mecano {
  id          Int      @id @default(autoincrement())
  firstName   String
  lastName    String
  email       String   @unique
  password    String
  createdAt   DateTime @default(now())
  mobile      String
  address     String
  city        String
  code_postal String
  horraire    String
  siret       String
  garage      String
}
